# REPORT.md

## Created
Public GitHub Pages starter repository.

## Confirmed
- Episode structure supports `ep01`, `ep02`, etc.
- Episode page uses public JSON and neutral image IDs.
- EP01 final WebP asset package was generated locally as `kaiki_ep01_final_assets_v01_web.zip`.
- Final WebP assets were uploaded by the user.
- GitHub Pages real-device check was completed by the user and reported OK.
- `docs/index.html` includes a spoiler-free section titled `プレイヤー用　ChatGPT開始プロンプト`.
- `docs/index.html` now includes a spoiler-containing host section titled `出題者用　一台完結プロンプト`.

## Current asset status
- EP01 is now using the final WebP asset set uploaded by the user.
- The previous SVG files remain historical placeholders only and are not the intended current display assets.

## Current prompt status
- Public index includes a player-facing ChatGPT start prompt.
- Public index also includes a host-only one-chat prompt inside a spoiler warning / details section.
- The host-only prompt contains hidden truth, judgment rules, and endings, so it is intentionally public for family-test convenience only.
- For stricter spoiler protection, move the host prompt back to private or to a dedicated GPT later.

## Unconfirmed
- Assistant has not independently opened the live GitHub Pages page after the host prompt update.
- iPhone Safari display and copy behavior of the host prompt section has not yet been confirmed.
- Family playtest result has not yet been recorded.

## Follow-up
- Check the public index page on iPhone Safari.
- Confirm that the host prompt is hidden behind the details disclosure by default.
- Record family playtest results after running EP01.
- Add `ep02` by copying the ep01 structure when ready.